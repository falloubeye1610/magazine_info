

import 'package:flutter/material.dart';

class redacteurInterface extends StatelessWidget {
  const redacteurInterface({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text("Interface redacteur"),
    ),
    body: const Center(
      child: Text("Interface redacteur"),
    ),
    );
  
  }
}